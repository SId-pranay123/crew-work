// import React from 'react';
// import { Task } from '../models/task'; // Assuming Task model is correctly defined
// import TaskCard from './TaskCard'; // Import the TaskCard component, assuming it exists

// interface TaskColumnProps {
//     status: Task['status'];
//     tasks: Task[];
// }

// const TaskColumn: React.FC<TaskColumnProps> = ({ status, tasks }) => {
//     return (
//         <div className="task-column">
//             <h2 className="task-column-title">{status}</h2>
//             <div className="task-list">
//                 {tasks.map((task) => (
//                     <TaskCard key={task.id} task={task} />
//                 ))}
//             </div>
//             <button className="btn btn-add-task" onClick={() => {/* logic to add new task */}}>Add new</button>
//         </div>
//     );
// };

// export default TaskColumn;

